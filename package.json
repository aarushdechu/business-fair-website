{
  "name": "a-sketchy-business",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check server/app.js && node --check server/auth.js && node --check server/config.js && node --check server/database.js && node --check server/email.js && node --check server/orders.js && node --check server/utils.js && node --check public/app.js && node --check public/service-worker.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "google-auth-library": "^10.9.1",
    "nodemailer": "^9.0.5",
    "pg": "^8.16.3"
  }
}
